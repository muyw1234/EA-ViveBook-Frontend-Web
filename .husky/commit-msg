COMMIT_MSG=$(head -n 1 "$1")

case "$COMMIT_MSG" in
  v*|V*)
    echo "Commit de versión detectado. Ejecutando tests..."
    npm run test
    ;;
  *)
    echo "Commit normal. No se ejecutan los tests."
    ;;
esac